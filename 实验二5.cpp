#include<iostream>
using namespace std;
/*int main() {
	char a = 0;
	int b = 0;
	int c = 0;
	int d = 0;
	int e = 0;
	cin.get(a);
	while (a != '\n') {
		if (a >= 'a' && a <= 'z' || a>='A' && a <= 'Z') {
			b++;
		}
		else if (a == ' ') {
			c++;
		}
		else if (a >= '1' && a <= '9') {
			d++;
		}
		else {
			e++;
		}
	cin.get(a);
	}
	cout << "英文字母的个数为：" << b << endl;
	cout << "空格的个数为：" << c << endl;
	cout << "数字字符的个数为：" << d << endl;
	cout << "其余字符的个数为：" << e << endl;
}*/